# To modify GRUB :
* edit /etc/default/grub
* run sudo grub-mkconfig -o /boot/grub/grub.cfg

# Manually enabled systemd services
dhcpcd.service
greetd.service
iwd.service
bluetooth.service

# Bluetooth
bluetoothctl, need to trust device

# Set this in /etc/bluetooth/main.conf
[Policy]
AutoEnable=true
[General]
DiscoverableTimeout = 0

# Wifi
iwctl

# Brightness permissions
sudo usermod -a -G video samuel
sudo usermod -a -G video greeter

# Git
git config --global user.email "samuel.gallay@gmail.com"
git config --global user.name "Samuel Gallay"
git config --global credential.helper store

# GPG
gpg --full-generate-key
gpg --list-secret-keys --keyid-format LONG